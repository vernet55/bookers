<h1>Books</h1>
<table>
 <thead>
  <tr>
   <th>Title</th>
   <th>Body</th>
  </tr>
 </thead>

  <tbody>
   <% @books.each do |book| %>

   <tr> <!--<tr>=tbodyでくくるときの横一列に表示する-->
    <td><%= book.title %></td>　<!--<td>=tbodyでくくったときの<tr>で横一列にしたものを縦に区切る-->
    <td><%= book.body %></td>

    <td><%= link_to "Show", book_path(book.id) %></td>  <!--(book.id)のbookは　each do |book|とイコール-->
    <td><%= link_to "Edit", edit_book_path(book.id) %></td>

    <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure？" %></td>
   </tr>

    <% end %>
  </tbody>
</table>


<h2>New book</h2>
<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>



<!-- form_with部分 -->
<%= form_with model: @book do |f| %>
  <div>
   <label for ="book_title">Title</label>
  <br>
   <%= f.text_field :title %>
  </div>

　<div>
   <lebel for ="Body_body">Body</lebel>
  <br>
   <%= f.text_area :body %>
 </div>

  <div>
   <%= f.submit 'Create Book' %>
  </div>
<% end %>